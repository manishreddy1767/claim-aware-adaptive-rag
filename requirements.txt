# Core (install PyTorch first; see README for the CUDA/CPU wheel command)
torch>=2.9
sentence-transformers>=3.0
transformers>=4.44
numpy>=1.26
pypdf>=5.0
python-docx>=1.1
beautifulsoup4>=4.12
requests>=2.31
# Optional: OCR for scanned PDFs (pure pip, no system install)
pypdfium2>=4.30
rapidocr>=3.0
onnxruntime>=1.20
# Optional: JavaScript-rendered webpages (then run: python -m playwright install chromium)
playwright>=1.45
# Web UI
streamlit>=1.38
pandas>=2.2
# Development / tests
pytest>=8.0
fpdf2>=2.7
